<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.info.note">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NoteActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NotePageActivity" />
        <activity android:name=".NewNotePageActivity" />
        <activity android:name=".ReminderActivity"></activity>
        <activity android:name=".RemindersActivity"></activity>
        <activity android:name=".AddReminderActivity"></activity>
        <activity android:name=".RemindersActivity2"></activity>
        <activity android:name=".AddReminderActivity2"></activity>
    </application>

</manifest>